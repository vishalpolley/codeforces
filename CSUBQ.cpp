#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int N, Q, L, R;
    cin >> N >> Q >> L >> R;
    int arr[n+1]={0};
    int a, x, y, l, r;
    while(q--)
    {
        cin >> a;
        if(a==1)
        {
            cin >> x >> y;
            arr[x]=y;
        }
        if(a==2)
        {
            cin >> l >> r;

        }
    }
    return 0;
}

